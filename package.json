{
  "name": "imonadetox-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "web": "npm run dev --workspace @imonadetox/web",
    "mobile": "npm run start --workspace @imonadetox/mobile",
    "install-deps": "npm install"
  }
}
